% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_simplelm.R
\name{my_simplelm}
\alias{my_simplelm}
\title{Manual Simple Linear Regression (No lm())}
\usage{
my_simplelm(x, y, plot = TRUE)
}
\arguments{
\item{x}{Numeric vector (predictor)}

\item{y}{Numeric vector (response)}

\item{plot}{Logical. If TRUE, shows scatter plot with regression line.}
}
\value{
A list containing intercept, slope, fitted values, residuals, and R-squared.
}
\description{
Computes simple linear regression estimates manually using basic formulas.
}
\examples{
# Example 1: Basic usage
x <- 1:10
y <- 3 + 2*x + rnorm(10)
model <- my_simplelm(x, y)
model

# Example 2: Show fitted line on a plot
my_simplelm(x, y, plot = TRUE)

# Example 3: Using custom data
set.seed(123)
temp <- rnorm(20, mean = 30, sd = 5)
pressure <- 50 + 1.8 * temp + rnorm(20, 0, 2)
my_simplelm(temp, pressure, plot = TRUE)

# Example 4: Compare with built-in lm()
lm_fit <- lm(y ~ x)
lm_fit$coefficients

}
